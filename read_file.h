#ifndef READ_FILE_H
#define READ_FILE_H

#include <fstream>
#include <set>
#include <map>
#include <memory.h>
#include "public.h"

void read_file(std::string name, bool ** &conn, size_t &size);

#endif
